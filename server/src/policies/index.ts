import isFormActive from './is-form-active';
import rateLimit from './rate-limit';
import perFormRbac from './per-form-rbac';

export default {
  'is-form-active': isFormActive,
  'rate-limit': rateLimit,
  'per-form-rbac': perFormRbac,
};
